/*
  Netlify _headers: set security headers (including CSP) at the HTTP level.
  Place this file at the repository root so Netlify applies the headers to all responses.
  Adjust the CSP sources if you load assets from CDNs or third-party services.
*/

/* Match all paths */
/* Path must be exactly "/*" on its own line. Header lines are indented by one space. */

/*
  Strict-but-practical CSP below:
  - default-src 'self' (limit most resources to same origin)
  - script-src 'self' (no inline scripts; if you need inline scripts use nonces/hashes)
  - style-src 'self' (move inline styles to files or use hashes/nonces)
  - img-src 'self' data: https: (allow data: for inline images and https hosts if needed)
  - font-src 'self' data:
  - connect-src 'self'
  - frame-ancestors 'none'
*/

/*
/*
 Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data: https:; font-src 'self' data:; connect-src 'self'; frame-ancestors 'none'
 X-Content-Type-Options: nosniff
 X-Frame-Options: DENY
 Referrer-Policy: no-referrer-when-downgrade
 Strict-Transport-Security: max-age=63072000; includeSubDomains; preload
*/
